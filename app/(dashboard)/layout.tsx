import React from "react";

const Dashboard = ({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) => {
  /* 
        setPin("");
      }; */ return <div>{children}</div>;
};

export default Dashboard;
